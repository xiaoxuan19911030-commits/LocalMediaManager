# 0.5.0-02 Installed Scan Smoke

Status: Blocked - not an acceptance pass

Date: 2026-07-16

Commit under test: `f588bcc`

Branch: `sprint/0.5.0-02-task-lifecycle`

## Completed environment checks

- Created an independent-installation backup at `D:\Jvedio\Backups\Local Media Manager Next\0.5.0-02-pre-smoke-verified-20260716-222031`. Every current installation file hash matched its backup copy (38 source files; the backup also contains two transient `.vs` cache files).
- Built the current branch and created `Local Media Manager_0.4.3_x64-setup.exe` (21,033,530 bytes).
- Ran the NSIS installer silently against `D:\Local Media Manager Next`; exit code was `0`.
- Created an isolated smoke root at `D:\Local Media Manager Next Smoke\0.5.0-02-20260716-2222\installed-scan` with three physical `.mp4` sample files.
- Ran the installed Migration binary against an isolated database copy. Result: `Status=Completed`, `integrity=ok`, `foreignKeys=0`.
- Opened the installed desktop application. Its visible status was `Bridge connected`; no console window was present.
- Closed the application normally. No `local-media-manager` or `LocalMediaManager.Bridge` process remained.

## Blocked scan execution

The installed Bridge cannot be launched with the isolated database and its required `LMM_BRIDGE_TOKEN` in this execution environment: the process-launch request is blocked by the environment security policy before the Bridge process starts. This is an environment limitation, not a product result.

The smoke run therefore did not execute the following cases and they must remain unaccepted:

- `Pending -> Running -> Completed` for an installed scan;
- same-name rating restore;
- exactly-once automatic sync task creation;
- duplicate-scan import idempotency;
- pause, resume, cancel, and retry;
- scan task recovery after application restart;
- scan-task logs and final task persistence.

No source behavior, product database, roadmap, TODO, changelog, or Feature Parity Matrix status was changed by this blocked run. The task lifecycle and library scan entries remain partially migrated until the listed installed scan cases have real evidence.
