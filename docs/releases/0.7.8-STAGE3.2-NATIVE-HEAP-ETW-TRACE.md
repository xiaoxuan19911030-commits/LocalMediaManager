# Local Media Manager v0.7.8 Stage 3.2 Native Heap ETW Trace

**Task:** `v0.7.8-STAGE3.2-NATIVE-HEAP-ETW-TRACE`
**Status:** BLOCKED before collection
**Date:** 2026-07-26
**Branch:** `codex/sprint-v0.7.8-stage3.1-bridge-memory`
**Bridge commit:** `17a7ff736e2fa90f51888689e1d5bd23675fa3ac`

## 1. Execution Environment

| Item | Result |
|---|---|
| Windows | Windows 11 Pro, `10.0.26200` (build `26200`) |
| WPR | `10.0.26100.8875` at `C:\Windows\System32\wpr.exe` |
| WPA | Not found on this machine |
| Administrator permission | **No** |
| Permission check | `net session` returned `System error 5 has occurred. Access is denied.` |
| ETW profile | Not started |
| Bridge PID / EXE | Not started for Stage 3.2 |
| ETL file / size | Not created |
| Symbols | Not configured because no trace was started |
| Isolated artifact root | Reserved: `D:\LocalMediaManager\artifacts\validation\v0.7.8-stage3.2-native-etw\` |

The task requires an elevated session before ETW Heap or Virtual Allocation collection. The permission check failed, so no WPR profile was started and no workaround, privilege bypass, policy change, or alternative tracing method was attempted.

## 2. Pre-Trace Safety Checks

| Check | Result |
|---|---|
| Repository | `D:\LocalMediaManager` |
| Expected Stage 3.1 diagnostic commit | HEAD is `17a7ff736e2fa90f51888689e1d5bd23675fa3ac` |
| Stage 3.1 rollback tag | `rollback/v0.7.8-stage3.1-memory-pre` -> `8c380bd5d9854aa5f41f1510306ea5e0d2fa1f57` |
| Stage 3.2 rollback tag | `rollback/v0.7.8-stage3.2-native-etw-pre` -> `17a7ff736e2fa90f51888689e1d5bd23675fa3ac` |
| Existing user worktree changes | Preserved; not staged, reverted, or committed |
| Existing artifacts | Preserved and excluded from Git |
| Prior Stage 3.6 isolated HeapWalk processes | Known PIDs `28692` and `11156` were no longer running; no termination was needed |
| Formal database | Not opened, hashed, copied, or modified by Stage 3.2 |
| Formal deployment directory | Not accessed or deployed |

## 3. Memory Baseline

No Stage 3.2 Bridge process or fixed 100-title batch was started after the permission failure. Therefore the required Stage 3.2 points (`Start`, every 10 titles, `After 60s`, and `After Full GC`) are intentionally unavailable.

The prior Stage 3.1 evidence remains the only valid baseline:

| Point | Private commit |
|---|---:|
| Bridge start | 162.8 MiB |
| First 100-title batch after full GC | 354.9 MiB |
| Second 100-title batch after full GC | 471.7 MiB |

Stage 3.1 established native `MEM_PRIVATE` growth, with managed heap, `MEM_MAPPED`, and `MEM_IMAGE` not explaining the increase. It did **not** identify the allocator or call stack.

## 4. Heap Top Allocations

Not available. No Heap ETW trace was collected.

## 5. Virtual Allocation Top

Not available. No Virtual Allocation ETW trace was collected.

## 6. Module Attribution and Phase Correlation

Not available. Without an elevated ETW capture, no allocator, module, stack, outstanding allocation, release ratio, or synchronization-phase attribution can be claimed.

## 7. Root Cause Conclusion

`Trace Inconclusive`

The prior conclusion is retained: the observed growth is native private commit. The source remains unknown because the required Heap / Virtual Allocation evidence was not permitted in this session.

## 8. Fix Decision

`Additional Trace Required`

No production logic, provider behavior, synchronization semantics, caching, concurrency, resource downloads, database writes, or allocator-related dependency was changed. The uncommitted diagnostic worktree changes that predate this Stage 3.2 permission check remain unstaged and are not accepted as a fix.

## 9. Production Readiness

`BLOCKED - Native Allocator Still Unknown`

| Gate | Status |
|---|---|
| Human Acceptance | BLOCKED |
| Production Full Sync | BLOCKED |
| Formal database write | BLOCKED |
| Formal directory deployment | BLOCKED |

## 10. Required Next Step

Run Stage 3.2 from an elevated Windows PowerShell or elevated Codex desktop session. First verify elevation with `net session`, then collect one fixed isolated 100-title batch with WPR Heap tracing (and Virtual Allocation tracing when supported), retaining all ETL and diagnostic artifacts only below the Stage 3.2 artifact root. The trace must be analyzed before any production-memory fix is proposed.

## 11. Build and Test

No code was added or changed for this blocked Stage 3.2 result. No builds or tests were run after the access-denied gate because ETW collection never began.

## 12. Git Scope

This report is intentionally limited to the Stage 3.2 permission gate. It does not include ETL files, dumps, isolated databases, downloaded assets, user data, or the pre-existing dirty worktree changes.
