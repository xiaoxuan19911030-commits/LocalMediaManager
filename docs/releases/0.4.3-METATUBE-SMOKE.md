# Local Media Manager 0.4.3 MetaTube real smoke

## Scope

- Executed: 2026-07-16 14:42–14:45 (Asia/Shanghai)
- Provider: local MetaTube `http://127.0.0.1:8080/`
- Samples: 30 deterministic records from a SQLite backup of the product database
- Product database access: read-only SQLite backup API
- Isolated run root: `D:\Local Media Manager Next Smoke\0.4.3-live-r2-20260716`
- Isolated outputs: database, media placeholders, images, NFO, logs and evidence all stayed under the run root
- No production media, image or NFO path was written

Per-movie before/after snapshots and titles remain in the local evidence directory because they contain private library data. They are intentionally excluded from the public repository.

## Result

| Measure | Result |
|---|---:|
| Total | 30 |
| Completed | 22 |
| Failed | 8 |
| Explicit no result | 8 |
| Network/provider request failure | 0 |
| Provider code mismatch accepted | 0 |
| Images registered | 311 |
| NFO documents registered | 22 |
| User state and user-tag sets preserved | 30/30 |
| Existing images preserved by hash | 5/5 |
| Existing user NFO preserved byte-for-byte | 5/5 |
| Temporary files remaining | 0 |
| Retried tasks | 1 |
| `PRAGMA integrity_check` | `ok` |
| `PRAGMA foreign_key_check` rows | 0 |

The eight failed samples were valid, explicit `MetaTube 未找到 ... 的结果` outcomes. A Provider search `404` is now classified as an empty result without three pointless retries. It is not reported as a successful sync and does not write metadata, images or NFO.

## Task controls

The isolated real run recorded all of the following in persistent task logs:

- pause;
- resume;
- cancel;
- retry;
- normal completion;
- explicit no-result failure.

An interrupted-running-task recovery case is additionally covered by the automated Bridge suite. On executor startup, an interrupted synchronization moves to `Retrying`, increments `RetryCount` and writes an `上次异常中断` task log.

## Protection evidence

- Favorites, explicit ratings, notes and user-created tag sets were compared before and after for every sample; all 30 were unchanged.
- Five pre-existing valid image files were hashed before execution and matched afterward.
- Five pre-existing user NFO files were compared byte-for-byte and matched afterward.
- New downloads used isolated temporary directories; no `.part` or `.lmm-temp` file remained.
- The NFO output-directory setting was exercised by the real run and now controls automatic Provider writes.

## Local evidence

The private local evidence set contains:

- `evidence/before.json`
- `evidence/after.json`
- `evidence/tasks.json`
- `evidence/task-logs.json`
- `logs/bridge-stdout.log`
- `logs/bridge-stderr.log`
- the isolated database and generated image/NFO directories

## Acceptance

The real MetaTube data path, non-destructive merge, image validation/save path, NFO path, task persistence and database integrity gates passed for this sample. Freeze still requires the remaining full build, installed NSIS smoke test, Bridge Release lifecycle checks and version-document updates.
