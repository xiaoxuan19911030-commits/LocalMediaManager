# v0.7.9 Directory Migration Verification

## Completed migration

| Category | New path |
|---|---|
| Source and Git worktree | `D:\自用软件\源代码目录\本地媒体管理器` |
| Deployment | `D:\自用软件\部署安装目录\本地媒体管理器` |
| Formal data | `D:\自用软件\部署安装目录\本地媒体管理器\数据` |
| Test data and artifacts | `D:\自用软件\测试目录\本地媒体管理器` |
| Temporary build logs | `D:\自用软件\临时目录\本地媒体管理器` |
| Backup root | `D:\自用软件\备份目录\本地媒体管理器` |

The source worktree was moved in-place on `D:`. Its active branch and all pre-existing uncommitted changes were retained. Isolated validation artifacts were moved out of the worktree into the test root.

## Runtime path changes

- Bridge and Migration default data roots now use the formal deployment data root.
- MetaTube smoke defaults now use the formal database and test root.
- Tauri release logs now use the formal deployment data root.
- `scripts/deploy-local-release.ps1` deploys to the new root and creates strict versioned backups. Only new exact version directories such as `v0.7.9` are eligible for the three-version retention rule; migration-era backups with suffixes are excluded.

## Verification

| Check | Result |
|---|---|
| Git worktree and branch | PASS: `codex/sprint-v0.7.9-stage4.2-image-memory-growth-shape`; pre-existing dirty files retained |
| Debug Bridge and Migration builds | PASS |
| Release Bridge and Migration builds | PASS |
| Web build | PASS |
| Tauri Release and NSIS build | PASS |
| Bridge Release Tests | PASS: 465/465 |
| Deployment | PASS: new deployment root, backup `v0.7.9` |
| Application launch | PASS from new deployment root |
| Bridge health | PASS: `status=ok`, formal database path under the new deployment data root |
| API smoke | PASS: `/health`, `/api/dashboard`, and `/api/settings/all` each returned HTTP 200 |
| Test/formal-data isolation | PASS: 16,863 validation artifacts under the test root; none remain under source `artifacts/validation` |
| `git diff --check` | PASS |

`configDatabaseAvailable=false` remains the pre-existing runtime state: neither the migrated formal data root nor the deployment backup contained a direct runtime `config\app_configs.sqlite`; only historical copies exist under legacy backups. No configuration database was fabricated during migration.

## Retained old directories: deletion not approved

| Path | Status |
|---|---|
| `D:\LocalMediaManager` | Empty source-root shell left by the in-use directory move; 0 files |
| `D:\LocalMediaManager-release-v0.7.2` | Old source candidate retained for inventory and separate deletion approval |
| `D:\LocalMediaManager-v078-clean` | Old source candidate retained for inventory and separate deletion approval |
| `D:\Local Media Manager Next Data` | Contains only two pre-fix log files from the initial migration launch; no formal database or media data |
| Historical directories under `D:\自用软件\备份目录\本地媒体管理器` | Retained; no automatic cleanup was applied |

No old directory was deleted. A separate inventory and explicit confirmation are required before any cleanup.

## Human confirmation still required

The migrated historical backup collection is approximately 337 GiB. It is outside the automatic retention policy and must be scanned into a dated inventory with directory size, probable version, and retention recommendation before any deletion decision. The old source candidates and the small old log-only data directory likewise require a separate deletion approval.
