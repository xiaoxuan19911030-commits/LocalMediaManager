# Local Media Manager 0.4.1 Release Verification

## Release identity

- Version: `0.4.1`
- Lifecycle: `Planning → Design → Develop → Self Test → Smoke Test → Freeze → Release → Archive`
- Feature commit: `da9fcdf`
- Safety test commit: `cc99a9f`
- Release documentation commit: `f8a16d7`
- Rollback tag: `rollback/before-sprint-0.4.1-20260715`
- Installation root: `D:\Local Media Manager Next`
- Data root: `D:\Local Media Manager Next Data`

## Migration

- Migration: `0003_UserStateAuditAndRatingMemory`
- Temporary copy upgrade: applied version 3; `integrity=ok`; foreign keys `0`.
- Production verification: no pending migration; `integrity=ok`; foreign keys `0`.
- Production verification backup: `D:\Local Media Manager Next Data\data\backups\database\upgrade_20260715_204050\LocalMediaManager.db`.
- Pre-deployment application backup: completed under the private backup root; file count verified.
- Final pre-install application backup: completed under the private backup root; file count and total bytes verified.
- Final installed migration idempotence check: no versions reapplied; `integrity=ok`; foreign keys `0`; verification backup `upgrade_20260715_205102`.

## Automated self test

Command:

```text
dotnet test backend/LocalMediaManager.Bridge.Tests/LocalMediaManager.Bridge.Tests.csproj -c Release
```

Result: `6 passed, 0 failed`.

Covered behavior:

1. Favorite/rating persistence and unscored-versus-zero distinction.
2. Tag create, bind, batch bind, confirmed delete and rollback.
3. Deleted-rating restore without overwriting a newer rating.
4. Actor relationship transaction and playback history transaction.
5. Movie-record delete preview, database backup and filename-only rating memory.
6. ActorID=0 preview, repair task, relationship repair and database backup.

## Build verification

- `pnpm build:web`: passed; known non-blocking bundle-size warning remains.
- Bridge Debug/Release: passed, zero warnings/errors.
- Migration Debug/Release: passed, zero warnings/errors.
- `cargo check --manifest-path src-tauri/Cargo.toml`: passed.
- Bridge/Migration win-x64 publish: passed.
- Tauri Release + NSIS: passed.
- Installer: `Local Media Manager_0.4.1_x64-setup.exe`; silent install exit code `0`.
- Installed EXE file version: `0.4.1`.
- Final installed `/health`: version `0.4.1`, database available, write enabled, session authentication `X-LMM-Session`.

## Bridge integration smoke on a real database copy

- Health version `0.4.1`; writes enabled only with session token.
- Missing session token returned HTTP `401`.
- Favorite and rating 4.5 were written and read back from movie details.
- User tag was created, bound, impact-previewed and deleted.
- Actor details and movie-actor relationships completed a transaction round trip.
- Actor repair preview found zero production candidates and correctly performed no repair write.
- Neighbor context returned the expected previous and next records in a sanitized database copy.
- Controlled smoke player exited normally: play count and history total each increased by one.
- Movie deletion smoke on the copy: rating memory enabled, record returned HTTP `404` after confirmed removal, and an operation database backup existed.

## Installed UI smoke

- Dashboard loaded the complete local test library and Bridge connected.
- Movie wall loaded real covers, pagination data and batch-selection controls.
- Movie detail exposed rating, favorite, edit-tags, edit-actors and previous/next controls.
- A sanitized sample movie favorite was toggled, persisted, then restored to its original state; UI reported `已保存`.
- No user media file was opened or deleted during smoke testing.

## Known non-blocking follow-up

- The rating-restore Bridge operation is complete, but automatic invocation from scanning/import belongs to 0.4.2 because scanning/import is not yet migrated.
- Custom player-path persistence moves from the compatibility environment variable to Settings Service in 0.4.2.
- Vite route-level code splitting remains a performance backlog item.

## Release decision

0.4.1 satisfies the first legacy write-migration Sprint with the explicitly documented import-trigger follow-up. The final installer, installed health check and production Migration idempotence check passed; the version is frozen for release tagging.
