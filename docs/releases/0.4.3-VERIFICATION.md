# Local Media Manager 0.4.3 verification

Date: 2026-07-16  
Lifecycle: Freeze / Release acceptance  
Branch: `sprint/0.4.3-media-assets`

## Scope accepted

- protected Poster/Thumb/Fanart/BigPic/ExtraPic and actor-image workflow;
- derived thumbnail cache inspect, cleanup and rebuild;
- NFO import/export, Provider output, ownership and non-overwrite behavior;
- file organizer `Dry Run → Preview → Confirm → Task Execute` with compensation recovery;
- 30-title isolated real MetaTube workflow;
- playback Settings Service integration;
- Release Bridge background lifecycle and installed UI acceptance.

## Automated gates

| Gate | Result |
|---|---|
| `pnpm build:web` | Passed; existing Vite 500 KB chunk warning remains tracked in TODO |
| Bridge tests, Debug | 31/31 passed |
| Bridge tests, Release | 31/31 passed |
| Bridge build, Debug/Release | Passed, 0 errors |
| Migration build, Debug/Release | Passed, 0 errors |
| Rust tests, Debug | 1/1 passed |
| Rust tests, Release | 1/1 passed |
| Tauri Debug build | Passed |
| Tauri/NSIS Release build | Passed |
| `git diff --check` | Passed |

## Real Provider acceptance

The isolated 30-title run is documented in [`0.4.3-METATUBE-SMOKE.md`](0.4.3-METATUBE-SMOKE.md).

- 22 completed and 8 explicitly returned no result;
- no network/request failure was disguised as success;
- 311 image assets and 22 NFO documents were registered;
- user state and user tags remained unchanged for 30/30 samples;
- five locked images and five user NFO files remained byte-for-byte unchanged;
- database integrity passed and foreign-key violations were zero;
- pause, resume, cancel, retry and persistent task logs were exercised.

Private per-title codes and before/after evidence remain only in the isolated local smoke directory and are not committed.

## Package and install acceptance

- Existing installation backup: `D:\Jvedio\Backups\Local Media Manager Next\0.4.3-predeploy-20260716-1450`.
- Backup/source file count at backup time: 31/31.
- Installer: `Local Media Manager_0.4.3_x64-setup.exe`.
- Installer size: 21,022,983 bytes.
- Installer SHA-256: `E3FE087CBF9E52E835D41EA3211CECFAAD0427BF12EF68E18281750C188230B8`.
- Silent installer exit code: 0.
- Installed Bridge health reported version 0.4.3 and the real library count of 2,500.
- Release Bridge process had no console window; a second app instance did not create a second Bridge.
- Normal application exit left no installed app or Bridge process.
- Bridge file log was created and the 5 MB rotation behavior passed its Rust test.

## Installed UI acceptance

The installed package was opened with the real database. The following were inspected without destructive writes:

- dark and light themes;
- playback settings through Settings Service;
- movie wall and movie detail with enlarged poster;
- selected-tags-first tag editor and searchable tag pool (cancelled without saving);
- image locks plus NFO import/export and file organizer entry points;
- Task Center status, progress, retry and log controls;
- Bridge connected status and version 0.4.3.

## Explicitly not claimed complete

- smart-card automatic crop and left/center/right correction;
- MetaTube sensitive Header/Cookie credential storage;
- installed custom-player executable launch;
- NAS disconnect and permission-failure organizer acceptance;
- independent image/NFO task runners for every operation;
- full 125%/150% Windows scaling and large-library performance baselines;
- legacy records whose source text is already malformed or incorrectly encoded.

These remain partial or open in the Feature Parity Matrix/TODO and are not represented as completed by the 0.4.3 release.
